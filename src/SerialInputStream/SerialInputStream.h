/**
 * Arduino IO
 * 
 * SerialInputStream
 *
 * A SerialInputStream obtains input bytes from a serial port.
 */

#ifndef __ARDUINO_IO_SERIAL_INPUT_STREAM_H__
#define __ARDUINO_IO_SERIAL_INPUT_STREAM_H__ 1

#include <InputStream.h>

class SerialInputStream : public InputStream {
};

#endif /* __ARDUINO_IO_SERIAL_INPUT_STREAM_H__ */
